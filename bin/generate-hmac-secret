#!/usr/bin/env bash

if test -z "$1" ; then
  printf "$(gettext "Generates an hmac secret\n\nUsage:\t%s \"shared_secret\" \"your_secret\"\n")" \
         ${0##*/}
  exit 0
fi

echo -n "$2" | openssl sha1 -hmac "$1" -binary | base64
